# Development Profile - localhost (Aiven MySQL)
spring.jpa.show-sql=false

spring.datasource.url=jdbc:mysql://mysql-28f165d4-arkadiusz-5848.i.aivencloud.com:23166/defaultdb?rewriteBatchedStatements=true&UseUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true
spring.datasource.username=avnadmin
spring.datasource.password=${AIVEN_DB_PASSWORD:}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.dialect.storage_engine=innodb
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database=default

spring.jpa.properties.hibernate.jdbc.batch_size=1000
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Port dla localhost
server.port=8081

spring.main.lazy-initialization=true

# Flyway Configuration (dla Dev - MySQL)
# Automatyczna konfiguracja Flyway jest wyłączona - używamy FlywayConfig.java
spring.flyway.enabled=false

# Auth / JWT
jwt.secret=${JWT_SECRET_BASE64:TXlTdXBlclNlY3JldEtleU15U3VwZXJTZWNyZXRLZXlNeVN1cGVyU2VjcmV0S2V5MTIzNDU2Nzg5MDEyMzQ=}
jwt.access-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:900000}
jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}
jwt.remember-refresh-expiration-ms=${JWT_REMEMBER_REFRESH_EXPIRATION_MS:2592000000}
app.auth.google-client-id=${GOOGLE_CLIENT_ID:}
app.auth.cookie-secure=${AUTH_COOKIE_SECURE:false}
app.auth.email-verification.code-ttl-minutes=${EMAIL_VERIFICATION_CODE_TTL_MINUTES:10}
app.auth.email-verification.max-attempts=${EMAIL_VERIFICATION_MAX_ATTEMPTS:5}
app.auth.email-verification.resend-cooldown-seconds=${EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
app.auth.email-verification.daily-send-limit=${EMAIL_VERIFICATION_DAILY_SEND_LIMIT:10}
app.auth.email-verification.log-plain-code=${EMAIL_VERIFICATION_LOG_PLAIN_CODE:true}
app.auth.password-reset.code-ttl-minutes=${PASSWORD_RESET_CODE_TTL_MINUTES:10}
app.auth.password-reset.max-attempts=${PASSWORD_RESET_MAX_ATTEMPTS:5}
app.auth.password-reset.resend-cooldown-seconds=${PASSWORD_RESET_RESEND_COOLDOWN_SECONDS:60}
app.auth.password-reset.daily-send-limit=${PASSWORD_RESET_DAILY_SEND_LIMIT:10}
app.auth.password-reset.log-plain-code=${PASSWORD_RESET_LOG_PLAIN_CODE:true}

# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=${MAIL_DEBUG:false}
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
app.mail.from=${MAIL_FROM:${MAIL_USERNAME:}}


























