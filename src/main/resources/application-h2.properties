# ============================================
# Konfiguracja H2 Database (tryb MySQL)
# Aby u\u017Cy\u0107: java -jar app.jar --spring.profiles.active=h2
# Lub w IDE: Run Configuration -> Active profiles: h2
# ============================================

# H2 Database Configuration
# \u26A0\uFE0F WA\u017BNE: Dla tabeli "user" (s\u0142owo kluczowe w H2) u\u017Cywamy cudzys\u0142ow\u00F3w w @Table
# \u26A0\uFE0F WA\u017BNE: U\u017Cywamy trybu "file" zamiast "mem" aby dane by\u0142y zachowane mi\u0119dzy uruchomieniami
# Baza b\u0119dzie zapisana w katalogu ./data/testdb.mv.db (tworzony automatycznie)
spring.datasource.url=jdbc:h2:file:./data/testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console (dost\u0119pna pod http://localhost:8081/h2)
# Aby si\u0119 zalogowa\u0107 u\u017Cyj:
# - JDBC URL: jdbc:h2:file:./data/testdb
# - User Name: sa
# - Password: (pozostaw puste)
spring.h2.console.enabled=true
spring.h2.console.path=/h2
spring.h2.console.settings.web-allow-others=false

# Port aplikacji (dla H2)
server.port=8081

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false
# \u26A0\uFE0F WA\u017BNE: Automatyczne dodawanie cudzys\u0142ow\u00F3w do identyfikator\u00F3w (dla tabeli "user")
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Flyway Configuration (dla H2)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Logging - wy\u0142\u0105czone szczeg\u00F3\u0142owe logi Hibernate (mniej log\u00F3w w konsoli)
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.hibernate=WARN
logging.level.org.springframework.orm.jpa=WARN

# Auth / JWT
jwt.secret=${JWT_SECRET_BASE64:TXlTdXBlclNlY3JldEtleU15U3VwZXJTZWNyZXRLZXlNeVN1cGVyU2VjcmV0S2V5MTIzNDU2Nzg5MDEyMzQ=}
jwt.access-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:900000}
jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}
jwt.remember-refresh-expiration-ms=${JWT_REMEMBER_REFRESH_EXPIRATION_MS:2592000000}
app.auth.google-client-id=${GOOGLE_CLIENT_ID:}
app.auth.cookie-secure=${AUTH_COOKIE_SECURE:false}
app.auth.cookie-samesite=${AUTH_COOKIE_SAMESITE:Lax}
app.auth.allowed-origins=${AUTH_ALLOWED_ORIGINS:http://localhost:4200,https://angular-nowoczesne-af04d5c56981.herokuapp.com}
app.auth.email-verification.code-ttl-minutes=${EMAIL_VERIFICATION_CODE_TTL_MINUTES:10}
app.auth.email-verification.max-attempts=${EMAIL_VERIFICATION_MAX_ATTEMPTS:5}
app.auth.email-verification.resend-cooldown-seconds=${EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
app.auth.email-verification.daily-send-limit=${EMAIL_VERIFICATION_DAILY_SEND_LIMIT:10}
app.auth.password-reset.code-ttl-minutes=${PASSWORD_RESET_CODE_TTL_MINUTES:10}
app.auth.password-reset.max-attempts=${PASSWORD_RESET_MAX_ATTEMPTS:5}
app.auth.password-reset.resend-cooldown-seconds=${PASSWORD_RESET_RESEND_COOLDOWN_SECONDS:60}
app.auth.password-reset.daily-send-limit=${PASSWORD_RESET_DAILY_SEND_LIMIT:10}
app.auth.password-reset.log-plain-code=${PASSWORD_RESET_LOG_PLAIN_CODE:true}

# Google Calendar OAuth (per użytkownik)
app.calendar.oauth.client-id=${GOOGLE_CALENDAR_CLIENT_ID:}
app.calendar.oauth.client-secret=${GOOGLE_CALENDAR_CLIENT_SECRET:}
app.calendar.oauth.redirect-uri=${GOOGLE_CALENDAR_REDIRECT_URI:http://localhost:8081/api/calendar/oauth/callback}
app.calendar.oauth.scope=${GOOGLE_CALENDAR_SCOPE:https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/calendar.freebusy https://www.googleapis.com/auth/userinfo.email openid}
app.calendar.oauth.state-secret=${GOOGLE_CALENDAR_STATE_SECRET:change-me-h2-state-secret}
app.calendar.oauth.state-ttl-seconds=${GOOGLE_CALENDAR_STATE_TTL_SECONDS:600}
app.calendar.crypto.secret-base64=${GOOGLE_CALENDAR_CRYPTO_KEY_BASE64:}
app.calendar.default-calendar-id=${GOOGLE_CALENDAR_DEFAULT_ID:primary}# Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=${MAIL_FROM:${MAIL_USERNAME:}}